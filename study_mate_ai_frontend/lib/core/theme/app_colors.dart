import 'package:flutter/material.dart';

class AppColors {
  // Brand
  static const primary = Color(0xFF6C4CD2);
  static const primaryPressed = Color(0xFF5A3FBC);

  // Base
  static const background = Color(0xFFF7F5FB);
  static const surface = Color(0xFFFFFFFF);
  static const surfaceSoft = Color(0xFFF0ECFA);

  // Text
  static const textPrimary = Color(0xFF1C1B1F);
  static const textSecondary = Color(0xFF6B6676);

  // Lines
  static const outline = Color(0xFFDDD7EE);
  static const divider = Color(0xFFE7E1F3);

  // States
  static const success = Color(0xFF2EAD65);
  static const warning = Color(0xFFF5A524);
  static const error = Color(0xFFE5484D);

  // Optional accent (use rarely)
  static const tealAccent = Color(0xFF4FA7A1);
}
